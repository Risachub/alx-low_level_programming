#include "main.h"

/**
  *mul - multiplies two integers
  *@x: The first number to multiply
  *@y: The second number to multiply with the first num
  *Return: The result of multiplication of x and y
  */
int mul(int x, int y)
{
	return (x * y);
}
